# Virtual uv workspace root — not a package itself. A single member today
# (backend); the root keeps ./uv.lock and .venv at the repo root so tooling
# paths stay stable.
[tool.uv.workspace]
members = ["backend"]
